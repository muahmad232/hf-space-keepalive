name: Keep Hugging Face Space Alive

on:
  schedule:
    - cron: "*/30 * * * *"   # every 30 minutes
  workflow_dispatch:         # allows manual run

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Hugging Face Space
        run: |
          curl -I https://muahmad123-dark-tower-chatbot.hf.space
